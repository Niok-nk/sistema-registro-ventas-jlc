---
import Layout from "../layouts/Layout.astro";
import Card from "../components/Card.astro";
import Button from "../components/Button.astro";
---

<Layout title="Inicio">
  <main class="container hero-container">
    <div class="hero-content">
      <h1 class="hero-title">Sistema JLC Ventas</h1>
      <p class="hero-subtitle">
        Plataforma de gestión de ventas para asesores.
      </p>

      <div class="hero-actions">
        <Button href="/login" variant="primary">Iniciar Sesión</Button>
        <Button href="/registro" variant="outline">Registrarse</Button>
      </div>
    </div>

    <div class="features-grid">
      <Card title="Frontend Astro">
        <p class="feature-text">
          Renderizado estático rápido y optimizado para máxima velocidad.
        </p>
      </Card>

      <Card title="Backend PHP">
        <p class="feature-text">
          API robusta con seguridad PDO y autenticación JWT.
        </p>
      </Card>

      <Card title="Diseño Azul">
        <p class="feature-text">
          Interfaz minimalista "Azul Nocturno" con UX moderna.
        </p>
      </Card>
    </div>
  </main>
</Layout>

<style>
  .hero-container {
    padding-top: 5rem;
    padding-bottom: 5rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  .hero-content {
    max-width: 600px;
    margin-bottom: 4rem;
    animation: fadeIn 0.8s ease-out;
  }

  .hero-title {
    font-size: 3rem;
    font-weight: 700;
    color: var(--color-accent);
    margin-bottom: 1rem;
    line-height: 1.2;
    letter-spacing: -0.02em;
  }

  .hero-subtitle {
    font-size: 1.25rem;
    color: var(--color-text-secondary);
    margin-bottom: 2.5rem;
    line-height: 1.6;
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .features-grid {
    display: grid;
    gap: 1.5rem;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    width: 100%;
    max-width: 1000px;
    animation: slideUp 0.8s ease-out 0.2s backwards;
  }

  .feature-text {
    color: var(--color-text-secondary);
    line-height: 1.6;
  }

  @media (max-width: 768px) {
    .hero-container {
      padding-top: 3rem;
      padding-bottom: 3rem;
    }

    .hero-title {
      font-size: 2.25rem;
    }

    .hero-subtitle {
      font-size: 1.125rem;
      margin-bottom: 2rem;
    }
  }

  @media (max-width: 480px) {
    .hero-title {
      font-size: 1.875rem;
    }

    .hero-actions {
      flex-direction: column;
      width: 100%;
    }
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  @keyframes slideUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
