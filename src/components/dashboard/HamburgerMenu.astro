---
// src/components/dashboard/HamburgerMenu.astro
---
<button id="hamburger-menu" aria-label="Abrir menú" aria-controls="responsive-sidebar" aria-expanded="false" class="hamburger-button">
  <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <line x1="3" y1="12" x2="21" y2="12"></line>
    <line x1="3" y1="6" x2="21" y2="6"></line>
    <line x1="3" y1="18" x2="21" y2="18"></line>
  </svg>
</button>

<style>
  .hamburger-button {
    display: none; /* Oculto por defecto */
    border: none;
    cursor: pointer;
    padding: 0.3rem;
    color: var(--color-text-main);
    background-color: var(--color-bg-card);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    width: 3em;

  }

  @media (max-width: 768px) {
    .hamburger-button {
      display: block; /* Visible solo en móvil */
      z-index: 1100; /* Asegura que esté por encima de otros elementos */
    }
  }
</style>
